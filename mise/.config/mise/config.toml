[tools]
node = "24.3.0"
python = "3.13.5"
watchexec = "latest"
# Gitea's CLI isn't published to GitHub, so grab can't reach it; gitea.com speaks
# the Forgejo releases API
"forgejo:gitea/tea" = { version = "0.15.1", api_url = "https://gitea.com/api/v1" }

